1. nLog:
nLog is used to log the Errors, Trace, Debug and Warnings. It creates log files in application root folder.

2. Testing:
Test cases has been written to test the functionality. 

3. App Settings:
App.Config contains expected/valid arguments to determine command type. Arguments are validated against the values stored in config.

What can be done to improve:
a. Need to upgrade application to latest version tof .NET.
b. Trace log can be configured to use Database instead of File System. 